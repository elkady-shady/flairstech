You can Find docker-compose file and the Dockerfile-world
Run docker-compose up to get the containers created and running
you can access the api using GET http://localhost:8080/v1/countries/{conuntryCode}
